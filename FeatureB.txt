class FeatureB {

    void execute(Object input) {

        if (validate(input)) {
            save(input);
        }
    }
    
    boolean validate(Object output) {
        // validation logic
    }

    void save(Object output) {
        // save result
    }
}
